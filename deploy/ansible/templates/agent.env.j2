AGENT_TOKEN={{ agent_token }}
AGENT_ADDR=:8090
WG_INTERFACE=wg0
# Wire these to your Xray management (gRPC HandlerService at 127.0.0.1:10085).
# {uuid} is substituted by the agent. Left empty → VLESS ops return 501.
XRAY_ADD_CMD=
XRAY_REMOVE_CMD=

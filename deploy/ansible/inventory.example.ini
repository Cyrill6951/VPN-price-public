[vpn_nodes]
de-dev-01 ansible_host=203.0.113.10 ansible_user=root

[vpn_nodes:vars]
public_host=203.0.113.10

# WireGuard (server private key + short id come from `cmd/seed` output)
wg_server_private_key=REPLACE_WG_SERVER_PRIVATE_KEY
wg_subnet=10.7.0.0/24
wg_address=10.7.0.1/24
wg_port=51820

# VLESS + Reality
reality_private_key=REPLACE_REALITY_SERVER_PRIVATE_KEY
reality_short_id=REPLACE_SHORT_ID
reality_sni=www.microsoft.com
reality_dest=www.microsoft.com:443
reality_port=443

# Shared secret with the platform (PROVISIONER_AGENT_TOKEN)
agent_token=REPLACE_WITH_A_STRONG_TOKEN
